import * as React from 'react';
interface RenderVerticalLine {
    depth: number;
    depthGap: number;
    count: number;
    nodeHeight: number;
    verticalLineOffset?: number;
    verticalLineStyles?: React.CSSProperties;
    verticalLineTopOffset?: number;
}
export declare const VerticalLine: ({ depth, depthGap, count, nodeHeight, verticalLineOffset, verticalLineStyles, verticalLineTopOffset, }: RenderVerticalLine) => JSX.Element;
export {};
